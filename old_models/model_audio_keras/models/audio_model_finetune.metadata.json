{
  "model_name": "audio_model_finetune",
  "model_path": "models/audio_model_finetune.keras",
  "model_format": "keras",
  "sample_rate": 16000,
  "segment_duration_s": 3.0,
  "input_shape": [1, 160, 160, 3],
  "preprocessing": {
    "n_mels": 128,
    "n_fft": 2048,
    "hop_length": 512,
    "fmax": 8000,
    "scaling": "mel -> dB -> min-max per-segment -> scale 0..255 uint8 -> stack 3 channels -> resize 160x160 -> float32 -> MobileNetV2 preprocessing ((x-127.5)/127.5)"
  },
  "n_channels": 3,
  "labels_path": "models/training_files/labels.csv",
  "classes": [
    "ausencia_audio",
    "volume_baixo",
    "distorcido_clipping",
    "eco",
    "ruido_chiado",
    "sinal_teste"
  ],
  "notes": "Model may return logits or probabilities. Backend callers typically apply softmax when needed. Ensure labels.csv ordering is preserved."
}
